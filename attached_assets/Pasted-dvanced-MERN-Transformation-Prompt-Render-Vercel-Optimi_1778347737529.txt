dvanced MERN Transformation Prompt (Render & Vercel Optimized)
Role: Expert Full-Stack Developer & DevOps Architect
Objective: Restructure and optimize the existing MERN project for high-performance deployment (Backend on Render, Frontend on Vercel). The final code must be "Plug & Play"—ready for download and immediate deployment with ZERO manual code changes required after generation.

1. DECOUPLED ARCHITECTURE (Render & Vercel Ready)
Structure: Create two root-level folders: /frontend and /backend.

Backend (for Render):

Set up a standard Express structure in /backend.

Ensure PORT is dynamic: const PORT = process.env.PORT || 5000;.

Configure cors to accept requests from your Vercel URL and localhost.

Frontend (for Vercel):

Ensure all API calls use a central axios instance or a base URL variable: import.meta.env.VITE_API_URL.

Add a vercel.json in the /frontend folder to handle SPA routing (redirect all to index.html).

2. DESIGN & UI/UX UPGRADE (Modern & Professional)
Visual Styling: Use a modern color palette (e.g., Deep Indigo, Slate, and Emerald for success states).

Technology: Use Tailwind CSS or Advanced Custom CSS for a clean, responsive, and "SaaS-like" look.

UI Elements:

Implement Glassmorphism for cards and sidebars.

Add smooth hover animations and transition effects.

Design a professional "Status Badge" system for Club Join Requests (Pending/Approved/Rejected).

3. FEATURE INTEGRITY (Zero Changes to Logic)
Mandatory: Keep all existing features 100% intact. Do NOT remove or modify:

Club Management Logic.

Authentication Flow.

Cloudinary Image Uploads.

Member Request Systems.

Database: Use the existing MongoDB connection. Do NOT create new seeders or delete existing data.

4. ADVANCED FEATURES (Enhanced)
Forgot Password: A professional, secure flow with HTML email templates via Nodemailer.

Real-time Notifications: Automated emails to Club Admins when a student applies, including student details and a dashboard link.

Cloudinary: Ensure robust error handling for image uploads so the app doesn't crash on failed network requests.

5. DEPLOYMENT READY OUTPUT
The output must include:

Full Source Code: A complete, error-free zip-ready structure.

Deployment Guide:

Render (Backend): Specify the Build Command (npm install) and Start Command (node server.js or npm start).

Vercel (Frontend): Instructions to set the Root Directory to frontend and add the VITE_API_URL environment variable pointing to the Render URL.

Environment Variables Template: A clear list of all .env keys needed for both platforms.